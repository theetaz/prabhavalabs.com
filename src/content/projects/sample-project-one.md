---
title: 'Project One'
tagline: 'Replace me with your first open-source project'
description: 'This is a placeholder entry. Duplicate this file, rename it, and fill in the frontmatter — the site picks it up automatically. Set featured: true to promote it to the big showcase section.'
tags: ['TypeScript', 'Open Source']
repo: 'https://github.com/prabhavalabs'
status: 'active'
featured: true
order: 1
---

This body is the project's **story page** — everything below the frontmatter
renders at `/projects/sample-project-one` with full markdown support.

## The itch

Start with why the project exists. What annoyed you enough to build it? What
did you try first that didn't work?

## How it works

Explain the interesting parts. Code blocks are styled:

```ts
import { prabhava } from 'project-one';

const origin = prabhava.init({ openSource: true });
origin.ship();
```

> Blockquotes work too — good for pulling out the one-sentence philosophy of
> the project.

## Status & roadmap

Close with where the project is heading and how people can contribute.
